import {styled} from "styled-components";

export const StyledScore = styled.div`
    position: absolute;
    top: 30px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 24px;
    color: #fff;
    font-weight: bold;
    text-stroke: 1px #000;
`;
